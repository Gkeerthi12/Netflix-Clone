document.addEventListener("DOMContentLoaded", () => {
    const playButton = document.querySelector(".banner button");
    playButton.addEventListener("click", () => {
        alert("Play button clicked! Add your functionality here.");
    });
});